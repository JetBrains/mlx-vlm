"""The junie-mlx-vlm binary's own version.

Deliberately separate from ``mlx_vlm.version``, which tracks the upstream
mlx-vlm this fork follows. Bump this one when shipping a tarball from
build_cli_tarball.sh; it names the archive.
"""

__version__ = "0.1.0"
